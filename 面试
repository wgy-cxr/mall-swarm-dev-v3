简单做一个自我介绍：
    您好，我叫xxx,是西北工业大学计算机科学与技术大二的一名学生，在校自学了Java后端技术栈，包括Java基础，以及关系型数据库
    MySQL，非关系型数据库redis,mongdb，以及一些常用框架，springboot,mybatis,还有一些中间件rabbitmq,elasticsearch.在校做过
    两个项目。
介绍一下项目难点：
    首先第一个难点就是下单的时候，需要保证接口幂等性，还有就是有的商品需要一人一单。这两个难点放在一起说是因为我觉得这两个解决的思路
    大致相同。一人一单是对每个用户唯一，接口幂等性我理解的是指对每次下单唯一。所以我当时就考虑只用一个标识，标注他的唯一性就行。
    对于一人一单，因为userid这个属性每个用户只有一个，对用户是唯一的，所以我把userid作为唯一标识，传给后端，而接口幂等性是
    对每一单唯一，所以我的解决方案是前端每次点击提交订单的时候，先检查是否有订单标识这个变量，如果没有就创建，有就复用，当下单结束后，返回成功
    就删除这个标识。有了唯一标识后，到后端就可以用这个标识然后去用分布式锁。获取锁成功就执行，失败就返回以提交。
    我还想到一点就是，如果下单任务成功了，reids中的分布式锁也删掉了，这时候相同id的请求还有可能过来，我想到的场景有一人一单多次点击，因为网络波动
    有一个请求慢了，下单成功了都删除分布式锁了，这单才来，然后还有幂等性也是，也是因为网络波动，下单结束了，下个请求才来。这时候我还为订单加了一个唯一索引
    、
    一、整体流程（简版）
    text
    用户发起抢单请求
      ↓
    1. 幂等性校验（token / idempotent key）
      ↓
    2. 一人一单校验（Redis + 数据库双重）
      ↓
    3. 库存扣减（Redis 原子操作 / 数据库乐观锁）
      ↓
    4. 异步创建订单（MQ + 数据库落地）
      ↓
    5. 返回抢单结果
    二、核心实现要点
    1️⃣ 幂等性保证（必须放在最前面）
    推荐方案：请求唯一ID + Redis

    客户端生成 requestId（UUID / 业务唯一标识）

    服务端用 Redis SETNX 作为幂等令牌

    成功则执行后续，失败直接返回“重复请求”

    java
    String idempotentKey = "idempotent:" + requestId;
    Boolean success = redisTemplate.opsForValue()
            .setIfAbsent(idempotentKey, "1", Duration.ofMinutes(5));
    if (Boolean.FALSE.equals(success)) {
        return "请勿重复提交";
    }
    // 继续业务逻辑
    注意：幂等 key 必须在业务执行前 SETNX，并且业务失败要删除 key（允许重试）。

    2️⃣ 一人一单校验（并发核心）
    第一层：Redis 快速拦截（高并发）

    java
    String userLockKey = "user:order:" + userId;
    // 设置 1 秒过期，防止恶意重复请求
    Boolean acquired = redisTemplate.opsForValue()
            .setIfAbsent(userLockKey, "1", Duration.ofSeconds(1));
    if (Boolean.FALSE.equals(acquired)) {
        return "正在处理中，请勿重复抢单";
    }
    第二层：数据库唯一约束（绝对保证）

    sql
    ALTER TABLE orders ADD UNIQUE KEY uk_user_activity (user_id, activity_id);
    插入订单时数据库会抛出唯一约束异常，这是最终兜底。

    3️⃣ 库存扣减（不超卖）
    推荐：Redis Lua 脚本（原子操作）

    lua
    local key = KEYS[1]
    local userId = ARGV[1]
    local limit = tonumber(ARGV[2])

    local current = redis.call("incr", key .. ":count")
    if current > limit then
        redis.call("decr", key .. ":count")
        return 0
    end
    return 1
    或者使用 数据库乐观锁（适合非极端并发）：

    sql
    UPDATE product SET stock = stock - 1
    WHERE id = #{productId} AND stock > 0;
    -- 影响行数 = 0 表示扣减失败
    4️⃣ 异步落库（削峰）
    抢单成功 → 发 MQ → 消费者创建订单（校验唯一索引）

    java
    // 抢单成功，发送消息
    OrderMessage msg = new OrderMessage(userId, productId, requestId);
    mqProducer.send(msg);
    return "抢单中，请稍后查询结果";
    好处：

    数据库压力可控

    允许短暂延迟，不阻塞用户请求


    另一个是缓存与数据库的一致性问题：按理说只要保证修改缓存和数据库的时候，别去读数据，就能保证缓存与数据库的一致性，所以这里
    可以用读写锁，就是读的时候不能写，写的时候不能读，这样就可以保证数据的一致性。但是我了解到写线程可能“饿死”
    这是最经典的弊端。如果读操作非常频繁，写锁可能长时间获取不到锁，导致写线程一直等待，甚至永远无法执行。虽
    然有些实现（如 std::shared_mutex 的公平版本）能缓解，但默认实现通常偏向读者。很明显我的项目是缓存热点商品，比如优惠券
    他的读操作就很频繁，而且他也不需要保证强一致性。所以我采用的是另一种方法就是先修改数据库，在删除缓存，一段事件后在删除缓存。
    先删处缓存，在修改数据库，可能发生如下情况：线程一先删除了缓存，然后线程而过来读，发现缓存中没有，于是就去数据库中读到数据，
    此时线程一修改了数据，线程二将脏数据写到了缓存里，虽然第一种方法也可能会造成最终不一致，但是概率很小，需要同时满足修改的时候缓存中
    的数据刚好过期，另外读到数据到写道缓存的时间要大于修改数据库加删除缓存。所以概率小很多。然后后面那一次删除就是为了再次减少
    这个概率。
    面试官可能追问延时双删的时间间隔怎么确定。其实这个是很难确定的，因为拿我的第一种方案举例吧，如果发生了那种情况，如果从读数据
    到写到缓存大于修改数据库加删除缓存的话，大概率是redis出问题了，也有可能是与redis网络连接出现波动。这里其实我是没有好的解决方案的
    我认为这里应该要监控调整，定期监控一下缓存与数据库的不一致率，如果大于一定概率，就适当延长一下双删时间。而且还有过期时间作为
    兜底，这是我对这种方案的理解。


    面试官问数据，就这么回答
      使用 JMeter 对核心链路进行压测，在 16 核 CPU、32G 内存环境下：

      商品查询接口峰值达到 3000+ QPS（Redis缓存命中）

      Elasticsearch 搜索接口达到 500+ QPS
      下单接口稳定支撑 500 TPS，并保证库存不超卖
    面试官问支付，就回答好像支付宝支付我申请不了，我就用了在用户表中插入一个余额字段，去模拟支付。


    MongoDB
    常用注解对照表：
    注解
    作用
    对应 MongoDB
    @Document
    标记为文档实体
    Collection（集合）
    @Id
    主键
    _id
    @Indexed
    创建索引
    db.collection.createIndex()
    @Field("custom_name")
    自定义字段名
    字段别名
    @Transient
    忽略该字段
    不存储到数据库

    mongdb接口中定义的函数可以通过动态代理自己实现。还有MongoDB的id可以自动生成。

    ┌─────────────────────────────────────────────────────────┐
    │              Spring Bean 生命周期（完整版）               │
    ├─────────────────────────────────────────────────────────┤
    │                                                          │
    │  1. 实例化（Instantiation）                              │
    │     ↓                                                    │
    │  2. 属性赋值（Populate Bean）                            │
    │     ↓                                                    │
    │  3.  Aware 接口回调                                      │
    │     - BeanNameAware.setBeanName()                        │
    │     - BeanFactoryAware.setBeanFactory()                  │
    │     - ApplicationContextAware.setApplicationContext()    │
    │     ↓                                                    │
    │  4. BeanPostProcessor.postProcessBeforeInitialization()  │
    │     ↓                                                    │
    │  5. 初始化（Initialization）                             │
    │     - @PostConstruct 注解方法                            │
    │     - InitializingBean.afterPropertiesSet()              │
    │     - init-method 自定义初始化方法                       │
    │     ↓                                                    │
    │  6. BeanPostProcessor.postProcessAfterInitialization()   │
    │     ↓                                                    │
    │  7. Bean 就绪（可以使用了）                               │
    │     ↓                                                    │
    │  8. 销毁（Destruction）                                  │
    │     - @PreDestroy 注解方法                               │
    │     - DisposableBean.destroy()                           │
    │     - destroy-method 自定义销毁方法                      │
    │                                                          │
    └─────────────────────────────────────────────────────────┘



    该项目整体逻辑：
        登录： SA-token登录，逻辑是项目启动的时候，用到了postconstruct注解，执行这个方法提前吧数据库中定义好的那个路径
        需要哪个资源放到redis中，每次请求都会刷新这个表，然后执行登录操作，这个操作在白名单中，不会被拦截，然后到了
        mall-admin模块或者mall-portal模块，然后执行登录操作，登录操作的逻辑是现根据账户查询密码，查不到返回错误，然后
        把密码与查到的密码比对，不对直接返回错误，成功的话，用SA-token执行登录操作，sa-token会生成一个session，然后把
        用户信息存到redis中（这里之所以会用redis是因为引入的依赖是redis的），key是sessionid+生成的一个用户id，然后
        会把用户id啥的封装成jwt（因为配置了用jwt），然后登录成功进行其他操作，会被拦截，然后执行鉴权操作，因为用户拥有的资源也在
        redis中存着，解析jwt令牌获得id然后从redis中读取用户拥有的资源，然后再从redis中读取提前存好的那张路径资源关系表，然后就可以
        鉴权了。


        mall-admin中的操作和注意细节：
        首先就是每个管理用户能看到什么。有一个菜单表和菜单角色关联表，有了这两个表就能确定某个角色他能进行哪些操作了，但是为什么
        有了资源表了，还要有菜单表。原因是菜单表是给前端看的，资源表是后端鉴权用的。
        然后admin端可以进行推荐商品的设置，以及订单相关操作（包括订单，退单，退单原因，订单设置（通过这里的设置，前台mq的
        死信队列的时间设计就可以用这个时间了）），还有商品的一些增删改查，以及发布一些优惠券。


        前台的主要操作和细节：
        首先登录跟admin一样，就是前台没有权限这一说，所以不需要鉴权。
        其次前台依次有如下操作，首先是支付功能，支付功能就说没办法申请支付宝sdk，于是用的模拟支付，而且支付是异步的过程，扣减
        通过mq的可靠性（复习一下mq的可靠性），不可能扣减多次。
        然后是用户的收藏，浏览历史，关注品牌等消息是存到mongodb中（momgodb再看一下）。
        然后就是订单相关操作，这里有一个难点就是一人一单和接口幂等性，这个回答就用上面的。注意这里是异步生成订单，
        异步修改订单状态，同步扣减库存，因为虽然mq能保证消息的可靠性，但是他还是会有失败的策略，有时需要人工去处理，但是扣减库存
        这是时间要求很紧，如果还没处理，这个失败的返回了支付成功，但是没有扣减库存成功，就会出现超卖。
        然后就是商品的一些操作，这里也有一个难点就是缓存与数据库的一致性，回答也用上面的。
        然后就是用户的一些操作包括优惠券的一些操作，收货地址的一些操作，以及用户的相关操作



        1、Redis 分布式订单号生成：采用 Redis 原子递增实现分布式订单号生成，替
        代数据库自增主键方案，消除高并发下单时的数据库行锁竞争，下单接口 TPS 提升 3 倍。
            这里用的是redis的哪个increment，改变redis缓存的库存（这里其使用的是增长，然后多出来的可以作为数据
            提供给公司使用），然后支付成功后再扣减mysql的库存，
            tps大约有600。

        2、RabbitMQ 延迟队列处理超时订单：基于死信队列实现订单超时自动取消，
        替代原有的定时轮询方案，数据库无效查询显著减少，大促场景下显著降低 DB 压力。

        3、两阶段库存扣减防超卖：采用预锁定库存 + 支付后扣减真实库存的两阶段方案，
        通过乐观锁替代行级悲观锁，降低数据库锁竞争，高并发下单不超卖。
        添加预锁定库存，我考虑了这一点，首先预锁定库存再redis中，他快，所以下单这个接口就会变快，其次如果直接扣减数据库中的数据
        如果下单之后，她不想要了，或者超时未支付，又要再修改数据库，给数据库造成压力。（这里可能会想到为什么不要下单了，直接支付
        ，下的订单是确定用户接收地址等信息的关键步骤，对企业也有用来分析处理的作用，不能少）执行支付功能时，用乐观锁扣减库存。

        4、网关层统一分布式鉴权：在 Spring Cloud Gateway 集成 Sa-Token，结合 Redis 缓存权限规则，
        将鉴权逻辑收敛在网关层，单次请求减少 1-2 次内部 RPC 调用，整体延迟降低 30%。

        Gateway
         ↓
        Order-Service
         ↓ RPC
        User-Service
         ↓ RPC
        Permission-Service
        传统的举个下单业务，首先他要先进入下单service,然后再去获取用户权限，这时候需要调用权限相关的service,用了这种方案
        后，就可以直接再网关层，直接鉴权，就不用进入到order service和权限相关service中。


6、Elasticsearch 分流搜索流量：将商品搜索从 MySQL 剥离至独立 ES 服务，
搜索流量不再占用主库连接资源，主库 QPS 降低 60%+，搜索响应时间从百毫秒降至十毫秒级。


8、Druid 连接池调优与慢 SQL 治理：通过 Druid 连接池参数调优与慢
 SQL 监控，定位并优化订单分页等慢查询接口，平均响应时间从 800ms 降至 150ms。
这里之所以是150ms是因为一些简单的查询都已经放到别的数据库中了，比如查询某个商品的详细信息是再redis中做了缓存，剩下的都是一些
增删改以及分页查询，分页查询耗时可能很高，所以看起来平均响应时间很多

 我认为连接池重要的属性有这么几个：一个是最大连接数，这个有一个公式就是最大连接数=qps*平均响应时间，所以我的最大线程数给定到了50；
 其次就是最小空闲连接数，这个大约是最大线程数的百分之20，初始创建的线程数大约等于最大线程数，不过这些一般都是动态调整的
 比如如果监控到发现大量线程等待链接，就说明最大线程数小了，需要调大。


 Nacos配置中心
     ↓
 配置变更（maxActive/minIdle等）
     ↓
 Nacos Push通知（长轮询 / UDP）
     ↓
 Spring Cloud Nacos Listener收到变更
     ↓
 触发 Environment Refresh（@RefreshScope）
     ↓
 Bean属性重新绑定
     ↓
 DruidDataSource.setMaxActive() 等方法更新
     ↓
 连接池参数生效（无需重启）


 旧 Bean → destroy
 ↓
 重新从容器创建
 ↓
 注入新配置
 ↓
 变成新 Bean


